import styled from "styled-components"
import {
  space,
  width,
  flexDirection,
  justifyContent,
  alignItems,
  flex,
  flexWrap,
  color
} from "styled-system"


export default styled.div`
  display: flex;
  position: relative;
  ${space};
  ${width};
  ${flexDirection};
  ${justifyContent};
  ${alignItems};
  ${flex};
  ${flexWrap};
  ${color};
`
